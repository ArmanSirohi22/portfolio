# Arman Sirohi — Portfolio

A responsive personal portfolio website for Arman Sirohi, Data Analyst | SQL · Power BI · Python · Excel.

## Files
- `index.html` — website structure/content
- `style.css` — responsive design and dark mode
- `script.js` — theme toggle, mobile navigation, current year
- `Arman_Sirohi_Resume.pdf` — resume used by the Download Resume button

## Publish on GitHub Pages
1. Create a new public GitHub repository (for example `portfolio`).
2. Upload all files from this folder.
3. Open **Settings → Pages**.
4. Under **Build and deployment**, choose **Deploy from a branch**.
5. Select the `main` branch and `/ (root)`, then Save.
6. GitHub will provide your live website URL.

## Important
The project buttons currently point to the main GitHub profile because the resume does not contain individual repository URLs. Once individual repository links are available, replace those URLs in `index.html`.
